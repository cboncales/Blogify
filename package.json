{
  "name": "blogify",
  "version": "1.0.0",
  "description": "A simple blog web application with authentication and CRUD operations",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-deps": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "blog",
    "react",
    "node",
    "postgresql",
    "jwt"
  ],
  "author": "",
  "license": "MIT"
}
